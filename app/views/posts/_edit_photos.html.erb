<%= form_tag("/posts/#{post_id}", method: :put, id: 'edit_photos') do %>
	<% photos.each_with_index do |photo, index| %>
    <%= render partial: 'photos/thumbnail', object: photo %>
	<% end %>
<% end %>